<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".Controladores.RedirectActivity"
        android:background="@color/colorBackGround">

    <Button
        android:text="@string/redirect_User"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginBottom="50dp"
        android:layout_marginTop="30dp"
        android:id="@+id/redirect_users"
        android:textColor="@color/colorText"
        android:textColorHint="@color/colorPrimary"
        android:background="@drawable/rounder_corner_button"
        android:ems="10"
        android:onClick="redirectUsers"/>

    <Button
        android:text="@string/redirect_Movies"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@+id/redirect_users"
        android:id="@+id/redirect_movie"
        android:textColor="@color/colorText"
        android:textColorHint="@color/colorPrimary"
        android:background="@drawable/rounder_corner_button"
        android:ems="10"
        android:onClick="redirectMovies"/>

</android.support.constraint.ConstraintLayout>